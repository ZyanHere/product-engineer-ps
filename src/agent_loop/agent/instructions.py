"""Versioned static agent instructions.

A first-class artifact, not an incidental string: every run records the version
it used, so behaviour is attributable to a specific contract.

Must NOT own: runtime behaviour.
"""
